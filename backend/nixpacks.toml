[phases.setup]
nixPkgs = ["maven", "jdk17"]

[phases.install]
cmds = ["mvn clean install -DskipTests"]

[phases.build]
cmds = ["mvn package -DskipTests"]

[start]
cmd = "java -jar target/backend-1.0.0.jar"

